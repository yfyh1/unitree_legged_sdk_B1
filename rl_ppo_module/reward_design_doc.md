# 奖励函数设计说明

## 1. 奖励组成
- **碰撞惩罚**：移动后超出边界或进入障碍物 → `-10`，终止
- **距离变化奖励**：有效移动时，`(old_dist - new_dist) * 2.0`，其中 dist = |x1-x2| + |y1-y2|
- **步数代价**：每步 `-0.5`
- **到达目标**：`agent_pos == goal_pos` → `+100`，终止

## 2. 公式总结
reward = (old_dist - new_dist) * 2.0 - 0.5 （有效移动）
若碰撞 → -10 且终止
若到达 → 100 且终止
